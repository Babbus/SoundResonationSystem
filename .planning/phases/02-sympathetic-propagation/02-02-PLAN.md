---
phase: 02-sympathetic-propagation
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - Assets/SoundResonance/Editor/TestSceneSetup.cs
autonomous: false
requirements: [ECS-04]

must_haves:
  truths:
    - "Test scene contains a tuning fork pair (two same-frequency objects near each other) plus a mismatched-frequency control object"
    - "Striking one tuning fork object causes its pair to vibrate sympathetically (visible in ECS inspector)"
    - "The mismatched-frequency control object does NOT vibrate when a tuning fork is struck"
  artifacts:
    - path: "Assets/SoundResonance/Editor/TestSceneSetup.cs"
      provides: "Extended test scene with tuning fork pair + mismatched control"
      contains: "TuningFork"
  key_links:
    - from: "TestSceneSetup.cs"
      to: "ResonantObjectAuthoring"
      via: "AddComponent call for scene objects"
      pattern: "AddComponent<ResonantObjectAuthoring>"
---

<objective>
Extend the Phase 1 test scene with a tuning fork pair demonstration and verify sympathetic propagation end-to-end in PlayMode.

Purpose: Provides a visual/manual verification scene that demonstrates the thesis headline feature -- striking one object causes its frequency-matched neighbor to vibrate while a mismatched control stays still.

Output: Updated TestSceneSetup.cs with tuning fork pair + control object, verified working in PlayMode.
</objective>

<execution_context>
@C:/Users/yaseb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/yaseb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-sympathetic-propagation/02-CONTEXT.md
@.planning/phases/02-sympathetic-propagation/02-01-SUMMARY.md

<interfaces>
<!-- Existing TestSceneSetup pattern for creating resonant objects -->

From Assets/SoundResonance/Editor/TestSceneSetup.cs:
```csharp
public static class TestSceneSetup
{
    private const string ScenePath = "Assets/Scenes/ResonanceTestScene.unity";
    private const string PresetPath = "Assets/SoundResonance/Runtime/ScriptableObjects/Presets";

    [MenuItem("Sound Resonance/Create Test Scene")]
    public static void CreateTestScene() { ... }

    private static void CreateResonantObject(string name, Vector3 position, Vector3 scale, MaterialProfileSO material) { ... }

    private static MaterialProfileSO LoadOrCreatePreset(string presetName) { ... }
}
```

Key: CreateResonantObject creates a Cube primitive with MeshFilter, MeshRenderer, BoxCollider, and ResonantObjectAuthoring. The authoring component is assigned a MaterialProfileSO. Scene uses NO SubScene.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend TestSceneSetup with tuning fork pair and mismatched control</name>
  <files>Assets/SoundResonance/Editor/TestSceneSetup.cs</files>
  <read_first>
    Assets/SoundResonance/Editor/TestSceneSetup.cs
    Assets/SoundResonance/Runtime/ScriptableObjects/MaterialProfileSO.cs
    Assets/SoundResonance/Runtime/ScriptableObjects/MaterialDatabase.cs
  </read_first>
  <action>
Modify `TestSceneSetup.CreateTestScene()` to add a tuning fork pair demonstration group after the existing 3 objects (SteelBar, GlassPlate, WoodBar). Keep all existing objects and code unchanged.

Add a comment section separator before the new objects:
```csharp
// --- Sympathetic Propagation Demo: Tuning Fork Pair + Control ---
```

**Add 3 new objects using the existing `CreateResonantObject` helper:**

1. **TuningForkA** -- Steel, placed at `(3.0f, 0.5f, 0f)`, bar shape `(0.1f, 0.1f, 0.5f)`:
```csharp
CreateResonantObject(
    name: "TuningForkA",
    position: new Vector3(3.0f, 0.5f, 0f),
    scale: new Vector3(0.1f, 0.1f, 0.5f),
    material: steel);
```

2. **TuningForkB** -- Steel (same material = same frequency), placed at `(4.0f, 0.5f, 0f)` (1 meter away), same shape `(0.1f, 0.1f, 0.5f)`:
```csharp
CreateResonantObject(
    name: "TuningForkB",
    position: new Vector3(4.0f, 0.5f, 0f),
    scale: new Vector3(0.1f, 0.1f, 0.5f),
    material: steel);
```

3. **MismatchedControl** -- Wood_Oak (different material = different frequency), placed at `(5.0f, 0.5f, 0f)` (nearby but different frequency), bar shape `(0.08f, 0.08f, 0.4f)`:
```csharp
CreateResonantObject(
    name: "MismatchedControl",
    position: new Vector3(5.0f, 0.5f, 0f),
    scale: new Vector3(0.08f, 0.08f, 0.4f),
    material: woodOak);
```

Update the `Debug.Log` at the end to include the new objects:
```csharp
Debug.Log($"[Sound Resonance] Test scene created at {ScenePath} with 3 resonant objects " +
          "(SteelBar, GlassPlate, WoodBar) and sympathetic propagation demo " +
          "(TuningForkA, TuningForkB, MismatchedControl) with StrikeInputManager.");
```

**Do NOT modify** the `CreateResonantObject` or `LoadOrCreatePreset` helper methods. The `steel` and `woodOak` variables are already loaded at the top of `CreateTestScene()`.

**Placement rationale:**
- TuningForkA and TuningForkB are 1 meter apart (within propagation range, same material so same frequency)
- MismatchedControl is 2 meters from TuningForkA (close enough to receive energy IF frequency matched, but wood has different frequency than steel)
- The group is offset at x=3.0 to visually separate from the Phase 1 objects at x=-1.0 to x=1.0
  </action>
  <verify>
    <automated>grep -n "TuningForkA" "Assets/SoundResonance/Editor/TestSceneSetup.cs" && grep -n "TuningForkB" "Assets/SoundResonance/Editor/TestSceneSetup.cs" && grep -n "MismatchedControl" "Assets/SoundResonance/Editor/TestSceneSetup.cs"</automated>
  </verify>
  <acceptance_criteria>
    - TestSceneSetup.cs contains `CreateResonantObject` call with name `"TuningForkA"` at position `(3.0f, 0.5f, 0f)` using `steel` material
    - TestSceneSetup.cs contains `CreateResonantObject` call with name `"TuningForkB"` at position `(4.0f, 0.5f, 0f)` using `steel` material
    - TestSceneSetup.cs contains `CreateResonantObject` call with name `"MismatchedControl"` at position `(5.0f, 0.5f, 0f)` using `woodOak` material
    - TuningForkA and TuningForkB use the same material (steel) and same scale
    - MismatchedControl uses a different material (woodOak)
    - Existing SteelBar, GlassPlate, WoodBar creation code is unchanged
    - Debug.Log message updated to mention propagation demo objects
  </acceptance_criteria>
  <done>TestSceneSetup.cs extended with tuning fork pair (TuningForkA + TuningForkB at same steel material, 1m apart) and mismatched control (MismatchedControl at wood material) for sympathetic propagation demonstration.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify sympathetic propagation in PlayMode</name>
  <what-built>Complete sympathetic propagation system: SympatheticPropagationSystem.cs slots into ECS pipeline, 3 PlayMode tests verify frequency selectivity and distance attenuation, test scene extended with tuning fork pair demo.</what-built>
  <how-to-verify>
    1. Open Unity Editor
    2. Run menu: Sound Resonance > Create Test Scene (regenerates scene with new objects)
    3. Enter PlayMode
    4. Click on TuningForkA -- observe in Entity Inspector that its CurrentAmplitude rises
    5. Without clicking TuningForkB, observe in Entity Inspector that TuningForkB's CurrentAmplitude also rises (sympathetic response)
    6. Observe that MismatchedControl's CurrentAmplitude stays at 0 or near-zero (frequency rejection)
    7. Open Unity Test Runner > PlayMode tab
    8. Run all tests -- all 6 EmitterLifecycleTests AND all 3 SympatheticPropagationTests should pass
    9. Confirm no console errors or warnings during playback
  </how-to-verify>
  <resume-signal>Type "approved" if sympathetic propagation works correctly, or describe issues observed</resume-signal>
</task>

</tasks>

<verification>
1. `grep -n "TuningFork" Assets/SoundResonance/Editor/TestSceneSetup.cs` confirms tuning fork pair objects exist
2. `grep -n "MismatchedControl" Assets/SoundResonance/Editor/TestSceneSetup.cs` confirms control object exists
3. Human verification in PlayMode confirms end-to-end sympathetic propagation works visually
4. All 9 PlayMode tests pass (6 existing + 3 new)
</verification>

<success_criteria>
- Test scene has 6 total objects: 3 original (Phase 1) + 3 new (tuning fork pair + control)
- Striking TuningForkA causes TuningForkB to vibrate sympathetically
- MismatchedControl does NOT vibrate when TuningForkA is struck
- All PlayMode tests pass (EmitterLifecycleTests + SympatheticPropagationTests)
- Human approves the visual demonstration
</success_criteria>

<output>
After completion, create `.planning/phases/02-sympathetic-propagation/02-02-SUMMARY.md`
</output>
